{
  "name": "nfc-tag-notion-api",
  "version": "1.0.0",
  "description": "This simple script has the functionality to run a simple EDGE function in Vercel when we scan a NFC TAG",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "nfc",
    "tag",
    "android",
    "notion"
  ],
  "author": "Davide Del Gatto",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.4.0"
  }
}
